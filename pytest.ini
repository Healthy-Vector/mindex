[pytest]
pythonpath = .
# 테스트는 tests/ 안에서만 찾는다.
#
# 이 설정이 없으면 pytest가 docs/ocr-benchmark/04_search_test.py 를 이름 규칙
# (*_test.py)으로 수집해 import한다. 그 스크립트는 import 시점에 공유 DB로
# 접속하고 임베딩 모델(2.2GB)을 로딩하므로, 테스트를 돌리는 것만으로 원격 DB를
# 건드리게 된다. CI에는 sentence-transformers도 없어서 수집 단계에서 깨진다.
testpaths = tests
