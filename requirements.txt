# --- Web / API ---
# fastapi 0.115 계열은 starlette <0.47을 끌어와 SER-008(pip-audit)에서 CVE 8건이 뜬다.
# 0.141 계열이 starlette 1.x를 허용해 전부 해소된다. 사용 API는 FastAPI/APIRouter/Depends뿐이라 영향 없음.
fastapi==0.141.*
uvicorn[standard]==0.32.*
python-multipart==0.0.*
pydantic==2.*
pydantic-settings==2.*

# --- DB ---
sqlalchemy==2.*
psycopg2-binary==2.9.*
pgvector==0.3.*
alembic==1.13.*

# --- PDF / 문서 파싱 (PyMuPDF 금지 — AGPL) ---
pdfplumber==0.11.*
pypdfium2==4.*

# --- AI / 오케스트레이션 ---
# 0.2/0.3 계열에 미패치 취약점 5건. 아직 코드에서 import하는 곳이 0건이라 지금이 올리기 가장 싼 시점이다.
# P3가 파이프라인을 얹기 전에 1.x API 기준으로 시작할 것 (0.2 → 1.x는 API 변경이 크다).
langgraph==1.2.*
langchain-core==1.5.*
openai==1.*
anthropic==0.*

# --- 유틸 ---
python-dotenv==1.*
httpx==0.27.*

# --- 테스트 / 품질 ---
# pytest 9로 올려야 PYSEC-2026-1845가 해소된다. pytest-asyncio 0.24는 pytest<9을 요구하므로 함께 올린다.
pytest==9.*
pytest-asyncio==1.*
ruff==0.6.*
pip-audit==2.*
