# OpenSQL 라이선스 — 절대 커밋 금지
licenses/
*_license.xml

# 환경 설정 (비밀번호·API 키)
.env
*.env
!.env.example
common.env
remote.env
patroni.config.env
openproxy.config.env

# 생성 설정 파일
patroni.yml
openproxy.toml

# SSH 키
id_rsa_opensql*

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 데이터 (합성 데이터 산출물은 커밋하지 않고 스크립트만 커밋)
data/generated/
*.pdf
*.docx

# Frontend (React/Vite)
frontend/node_modules/
frontend/dist/
frontend/.vite/

# 개인 작업 문서 — 커밋하지 않는다
#
# 세션 진행 기록과 미결 판단은 개인 보관이다. 팀 공유 데이터 모델 정본은
# docs/mindex_remastered.dbml이며, 설명은 docs/mindex DB 설명서.md에 둔다.
# SessionStart 훅이 아래 파일을 로컬에서 읽지만, 없으면 조용히 넘어간다.
docs/WORKLOG.md
docs/DECISIONS.md
docs/archive/

# 개인 메모 일반 규칙 — 새로 만들 때는 이 규칙을 쓴다
docs/personal/
*.local.md

# 개인 Claude Code 설정 (환경변수·권한 등). settings.json은 팀 공유 훅만 담는다
.claude/settings.local.json
CLAUDE.md

# 기타
.DS_Store
*.log
.idea/
.vscode/
