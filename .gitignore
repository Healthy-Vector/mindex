# OpenSQL 라이선스 — 절대 커밋 금지
licenses/
*_license.xml

# 환경 설정 (비밀번호·API 키)
.env
*.env
!.env.example
common.env
remote.env
patroni.config.env
openproxy.config.env

# 생성 설정 파일
patroni.yml
!ops/ha/patroni.yml
openproxy.toml

# SSH 키
id_rsa_opensql*

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 데이터 (합성 데이터 산출물은 커밋하지 않고 스크립트만 커밋)
data/generated/
*.pdf
*.docx
contract_Data/
*.zip

# 합성데이터 워크스페이스 전용 문서 — 재유입 방지용 (해당 폴더는 이미 제거함)
# 원본은 ../k-rights-synthetic-data 에 있으며 서비스 저장소에서는 관리하지 않는다.
# - docs/overview/ : Codex 작업 지시문 · Phase 진행관리 (이 저장소에 없는 파일 23개 참조)
#   → 서비스 내용은 docs/K-RIGHTS_SERVICE_OVERVIEW.md 로, 진행상태는
#     docs/synthetic_data/DATASET_STATUS.md 로 정리해 두었다
# - docs/synthetic_data/decisions/ : PDF 렌더링·제목 규칙 등 생성 파이프라인 결정 기록
docs/overview/
docs/synthetic_data/decisions/

# Frontend (React/Vite)
frontend/node_modules/
frontend/dist/
frontend/.vite/

# 기타
.DS_Store
*.log
.idea/
.vscode/

# OpenSQL 벤더 배포물 (티베로 비공개 — 절대 커밋 금지)
Tmax_OpenSQL_*/
opensql-pkg.tar.gz
*.tar.gz

# 자격증명
*credentials*
.pgpass
*.pgpass
secrets/

# 키 · 인증서
*.pem
*.key
*.p12
mindex-key*

# 백업 · 복구본
recover_test/
pgdata/
*.dump
*.backup
*.sql.gz

# AI 모델 · 임베딩 캐시
models/
*.bin
*.safetensors
*.gguf
embeddings_cache/

# 도커
docker-compose.override.yml
